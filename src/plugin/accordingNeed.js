import Vue from 'vue'
import {
  Button,
  FormModel,
  Input,
  Checkbox,
  Layout,
  Menu,
  Icon,
  Breadcrumb
} from 'ant-design-vue'

Vue.use(Button)
  .use(FormModel)
  .use(Input)
  .use(Checkbox)
  .use(Layout)
  .use(Menu)
  .use(Icon)
  .use(Breadcrumb)
